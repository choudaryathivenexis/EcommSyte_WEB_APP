Save the command-center office image in THIS folder as:

    command-center.jpg

Full path:
    static/assets/showcase/command-center.jpg

It powers the homepage "Global e-commerce acceleration" section
(#command-center). Recommended: JPG, landscape ~4:3 (e.g. 1600x1200 or
larger), optimized to well under ~500 KB for fast loading.

Until the file is added, the section shows a branded dark-gradient panel
(no broken image), so the page always looks intentional.

To use a different filename or format, update the url(...) in
static/styles.css under the ".showcase__media" rule.
